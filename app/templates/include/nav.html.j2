<nav class="navbar navbar-dark bg-dark" id="main-nav">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">PokeBattler</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasDarkNavbar" aria-controls="offcanvasDarkNavbar">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="offcanvas offcanvas-end text-bg-dark" tabindex="-1" id="offcanvasDarkNavbar" aria-labelledby="offcanvasDarkNavbarLabel">
      <div class="offcanvas-header">
        <h5 class="offcanvas-title" id="offcanvasDarkNavbarLabel">PokeBattler Menu</h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas" aria-label="Close"></button>
      </div>
      <div class="offcanvas-body">
        <ul class="navbar-nav justify-content-end flex-grow-1 pe-3">
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="{{url_for('auth.index')}}">Home</a>
          </li>
          {# <li class="nav-item">
            <a class="nav-link" href="#">Profile</a>
          </li> #}
          {% if current_user.is_anonymous %}
            <li class="nav-item">
              <a class="nav-link" href="{{url_for('auth.get_login')}}">Log in</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{url_for('auth.get_register')}}">Register</a>
            </li>
          {% else %}
            <li class="nav-item"> <a class="nav-link" href="/register">Edit Profile</a> </li>
            <li class="nav-item"> <a class="nav-link" href="{{url_for('poke.my_pokemon')}}">My Pokemon</a> </li>
            <li class="nav-item"> <a class="nav-link" href="{{url_for('poke.find_opponent')}}">Find opponent</a> </li>
            <li class="nav-item"> <a class="nav-link" href="{{url_for('poke.get_search')}}">Catch Pokemon</a> </li>
            {# <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Battling
              </a>
              <ul class="dropdown-menu dropdown-menu-dark py-0">
                <li><a class="dropdown-item py-3" href="{{url_for('poke.find_opponent')}}">Find opponent</a></li>
                <li><a class="dropdown-item py-3" href="{{url_for('poke.get_search')}}">Catch Pokemon</a></li>
                <li> <hr class="dropdown-divider"> </li>
                <li><a class="dropdown-item py-3" href="#">Something else here</a></li>
              </ul>
            </li> #}
            <li class="nav-item">
              <a class="nav-link" href="{{url_for('auth.get_logout')}}">Logout</a>
            </li>
          {% endif %}
        </ul>
        <hr class="py-2">
        <form class="d-flex" role="search" action="{{url_for('poke.get_search')}}" method="POST">
          <input class="form-control me-2" name="search" type="search" placeholder="Quick pokemon search" aria-label="Quick pokemon search">
          <button class="btn btn-success" type="submit">Search</button>
        </form>
      </div>
    </div>
  </div>
</nav>